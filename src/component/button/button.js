import {  Button } from 'native-base';

const CustomButton =() => {
    return(
        <Button mt="2" color="success.400">
            Register Now!
          </Button>
    );
}

export default CustomButton